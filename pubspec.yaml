name: spur_login_signup_redesign
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  # Add the image to the list of assets
  assets:
    - assets/images/background1.png
    - assets/images/logo.png
    - assets/images/background2.png
    - assets/images/background3.png
